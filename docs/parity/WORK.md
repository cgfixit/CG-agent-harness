# Parity execution state

Assignment: keep the fixed 48-action inventory honest against live harness
code. No phase or action is complete merely because this file exists.
Implemented ≠ native-verified.

Inspected: `22520f3ba5981c361ceb57dddd7ff96234a69290` (`origin/main`, Latest
`v0.1.12`). CyClaw reference pin unchanged:
`a414ba86ebf5f3c8bb901466c16b4f015bbc79c9`. No open PRs at inspection.
Operator checkouts outside this worktree were not modified.

This is a **content** reclassification (issue #32 Session B), not a SHA-only
bump. Row evidence is in [actions.json](actions.json); the readable table is
generated by `python3 scripts/parity-status.py`.

## What changed in this audit

FACT upgrades against tip (HTTP/fixture evidence, native still `unavailable`):

- **M2–M5** stay or become `adapted` with current routes, slash overlays, and
  tests (`tests/structured_memory.rs`, suggestions, Phase 7 corpus).
- **M1** remains `needs-extension`: fact rows still have no tags / confidence /
  provenance columns even though recall and FTS shipped.
- **A8/A9** file persona propose/apply/history/recovery is `adapted`
  (`tests/persona_controls.rs`). CyClaw DB-shadow is not claimed.
- **A5** password change and user disable exist; identity CLI / audit-summary
  CLI remain.
- **A7** SQLite accounts exist; PostgreSQL auth and personality DB do not.
- **L5** exact-tag inventory helper is on main (`tests/model_readiness.rs`) even
  though PR #30 merged into `codex/parity-foundations` rather than `main`.
  Optional provider diagnostics and native acceptance still block a full close.
- **L7** runtime skill selection fixtures exist; full upstream skill audit does
  not.

FACT non-upgrades (do not invent):

- **C1–C10** and **I1–I8** remain `missing`. Tip has no fsconnect, sqlconnect,
  netconnect, telegram, or opentweet package. The clone jail is not a general
  filesystem connector.
- Native column stays `unavailable` for every row. Chromium / HTTP tests are
  not WKWebView acceptance. See [DESKTOP_ACCEPTANCE.md](../DESKTOP_ACCEPTANCE.md).

Historical A3 (PR #29, managed dotenv on Unix `serve`) remains `implemented`
and is still present on tip.

## Native gate

The existing DESKTOP_ACCEPTANCE report is historical, not a tip-artifact
matrix. Backend or Chromium tests cannot close native interaction. Issue #32
Session D tracks an exact-artifact table separately.

## Next smallest work (outside this docs PR)

1. Issue #87 close-out documentation, then close #87.
2. `#86` residual: `.git` name-equivalent refusal on `read_file` / `stat_file`
   (basename deny does not catch `.git/config`). No open PR for that as of this
   SHA.
3. Issue #32 Sessions C–E: exhausted-run diagnostics child issue, native
   exact-artifact matrix, then rewrite/close the tracker.
4. Connector/integration actions stay missing until separately prioritized.
   Shared A1/C10/O6/O7 remain open until their consumers land.

The 48-action assignment is not complete. Missing connectors are not waived.

All database/service tests must use disposable environments. No external
Telegram/OpenTweet writes, real Dropbox sync, production database use,
persistent schedule activation, main push, merge or release publication is
authorized by this ledger refresh.
