debug
target
/target
**/*.rs.bk
*.pdb
**/mutants.out*/
.DS_Store
*.log
/upstream/
/desktop/target/
/desktop/gen/
/dist/

# Private runtime homes and chat history, including repo-local test homes.
.CGagentHarness/
sessions/
.CGagentHarness/sessions/
.env
.env.*
!.env.example
.CGagentHarness/logs
.CGagentHarness/memory
.CGagentHarness/*
# whatevs lol if something doesnt work from new profile do it carefully

# Fine-tune local artifacts created at repo root by docs/FINETUNE.md
# (mlx_lm.lora adapter_path ./adapters, fuse --save-path ./cgagent-fused, .venv).
/adapters/
/cgagent-fused/
/.venv/

# Running the in-tree python scripts (scripts/*.py) byte-compiles them here.
__pycache__/
*.pyc


# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Generated by cargo mutants
# Contains mutation testing data
**/mutants.out*/

# RustRover
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/
