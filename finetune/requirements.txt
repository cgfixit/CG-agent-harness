# Training-only dependencies for the CG-Agent mlx-vlm QLoRA fine-tune.
# Install in a venv on the Mac:  pip install -r finetune/requirements.txt
# The dataset builder (build_dataset.py) and mock server (mock_server.py) use only the
# Python standard library — they need none of these.
mlx-vlm[train]>=0.6.8
