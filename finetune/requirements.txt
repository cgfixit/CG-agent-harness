# Python deps for the fine-tune toolchain.
# The builder (build_dataset.py) and mock server (mock_server.py) use only the
# standard library — no install needed for those.
#
# Training requires mlx-lm (Apple Silicon only). Install in a venv on the Mac:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r finetune/requirements.txt
mlx-lm>=0.22.0
